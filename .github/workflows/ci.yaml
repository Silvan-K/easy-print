name: ci

run-name: ${{ github.actor }} triggered CI

on: [push]

permissions:
  contents: read
  
jobs:
  test-all:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - name: Set up env
        run: apt-get install cmake ctest
      - name: 
